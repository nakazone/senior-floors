# ============================================
# VARIÁVEIS DE AMBIENTE - HOSTINGER
# ============================================
# 
# INSTRUÇÕES:
# 1. Preencha os valores abaixo com suas informações reais
# 2. No painel do Hostinger, vá em: Node.js Web App → Environment Variables
# 3. Copie e cole cada variável abaixo (ou importe este arquivo se o painel permitir)
# 4. Substitua os valores entre < > pelos seus dados reais
#
# ============================================

# ============================================
# BANCO DE DADOS (OBRIGATÓRIO)
# ============================================
# Formato: mysql://USUARIO:SENHA@HOST:3306/NOME_DO_BANCO
# Exemplo: mysql://u123456789:MinhaSenh@123@localhost:3306/u123456789_seniorfloors
# 
# ONDE ENCONTRAR:
# - No painel Hostinger: Databases → MySQL Databases
# - Use o HOST que aparece (geralmente "localhost")
# - Use o USUÁRIO e SENHA do banco MySQL
# - Use o NOME_DO_BANCO (geralmente começa com seu usuário, ex: u123456789_seniorfloors)
DATABASE_URL="mysql://<USUARIO>:<SENHA>@<HOST>:3306/<NOME_DO_BANCO>"

# ============================================
# NEXT.AUTH - AUTENTICAÇÃO (OBRIGATÓRIO)
# ============================================
# Gere uma chave aleatória segura com: openssl rand -base64 32
# OU use o valor gerado abaixo (já foi gerado automaticamente)
NEXTAUTH_SECRET="<GERAR_CHAVE_ALEATORIA_AQUI>"

# URL base do site (sem barra no final)
NEXTAUTH_URL="https://www.senior-floors.com"

# ============================================
# SITE - CONFIGURAÇÃO (OBRIGATÓRIO)
# ============================================
# URL pública do site (usada em links, sitemap, etc.)
NEXT_PUBLIC_SITE_URL="https://www.senior-floors.com"

# ============================================
# GOOGLE ANALYTICS (OPCIONAL)
# ============================================
# Se você usa Google Analytics, adicione o ID aqui
# Formato: G-XXXXXXXXXX
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# ============================================
# GOOGLE SEARCH CONSOLE (OPCIONAL)
# ============================================
# Código de verificação do Google Search Console
# NEXT_PUBLIC_GOOGLE_VERIFICATION="seu-codigo-de-verificacao"

# ============================================
# FORMULÁRIOS (OPCIONAL)
# ============================================
# Se usar serviço externo para formulários (Formspree, SendGrid, etc.)
# NEXT_PUBLIC_FORM_ENDPOINT="https://formspree.io/f/seu-form-id"

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 
# 1. NUNCA compartilhe este arquivo com valores reais preenchidos
# 2. No Hostinger, as variáveis são configuradas individualmente
# 3. Certifique-se de que DATABASE_URL está correto (use "localhost" como HOST se for o padrão)
# 4. NEXTAUTH_SECRET deve ser uma string longa e aleatória
# 5. Após configurar, faça redeploy da aplicação
#
# ============================================
# COMO GERAR NEXTAUTH_SECRET:
# ============================================
# No terminal, execute:
# openssl rand -base64 32
#
# Copie o resultado e cole no valor de NEXTAUTH_SECRET acima
#
# ============================================
